pub mod perceptron;

//use ndarray::prelude::*;
//
// fn main() {
//     // 論理演算
//     for x1 in [0.0, 1.0].iter() {
//         for x2 in [0.0, 1.0].iter() {
//             let y = perceptron::perceptron::add(*x1, *x2);
//             println!("{} AND {} = {}", x1, x2, y);
//         }
//     }
//     for x1 in [0.0, 1.0].iter() {
//         for x2 in [0.0, 1.0].iter() {
//             let y = perceptron::perceptron::nand(*x1, *x2);
//             println!("{} NAND {} = {}", x1, x2, y);
//         }
//     }
// }
